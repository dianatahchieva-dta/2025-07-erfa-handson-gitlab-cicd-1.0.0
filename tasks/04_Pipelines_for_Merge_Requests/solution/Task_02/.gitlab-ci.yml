stages:
  - test

test-job-name:
  stage: test
  script:
    - echo "TOTAL      123      456      789      84%"
  coverage: '/TOTAL.+ ([0-9]{1,3}%)/'
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
