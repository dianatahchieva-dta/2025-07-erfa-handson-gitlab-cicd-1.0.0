stages:
  - demo

demo-job-1:
  stage: demo
  script:
    - echo "This job always runs"

demo-job-2:
  stage: demo
  script:
    - echo "This Job only runs on main"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
